<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:avalonia="clr-namespace:Material.Icons.Avalonia;assembly=Material.Icons.Avalonia"
             xmlns:viewModels="clr-namespace:OwlAssistant.ViewModels"
             mc:Ignorable="d" d:DesignWidth="350" d:DesignHeight="670"
             x:Class="OwlAssistant.Views.SensorInfoView"
             x:DataType="viewModels:SensorInfoViewModel">
    <Design.DataContext>
        <viewModels:SensorInfoViewModel />
    </Design.DataContext>

    <Grid RowDefinitions="Auto,Auto,*">
        <!-- 操作栏 -->
        <Border Grid.Row="0"
                BorderBrush="{DynamicResource SystemControlBackgroundAccentBrush}"
                Padding="16,12"
                BorderThickness="0,0,0,1">
            <Grid>
                <TextBlock Text="Sensor Information"
                           FontSize="18"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center" />
                <StackPanel Orientation="Horizontal" Spacing="5" HorizontalAlignment="Right">
                    <ToggleSwitch IsChecked="{Binding ToggleVisibility}"
                                  OffContent="" OnContent=""/>
                    <Button Width="40"
                            Height="40"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            CornerRadius="20"
                            Command="{Binding RefreshAll}"
                            BorderThickness="0">
                        <avalonia:MaterialIcon Kind="Refresh"
                                               Width="20"
                                               Height="20"
                                               Foreground="#1976d2" />
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 传感器卡片区域 -->
        <Grid Grid.Row="1" IsVisible="{Binding !ToggleVisibility}" Margin="20" VerticalAlignment="Center" ColumnDefinitions="*,20,*">
            <!-- 室内传感器卡片 -->
            <Border MinWidth="160" Grid.Column="0"
                    CornerRadius="12"
                    Padding="16"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    BorderBrush="{DynamicResource SystemControlBackgroundBaseMediumBrush}"
                    BorderThickness="1">
                <StackPanel Spacing="12">
                    <!-- 标题 -->
                    <StackPanel Orientation="Horizontal" Spacing="6" HorizontalAlignment="Center">
                        <avalonia:MaterialIcon Kind="Home"
                                               Width="16"
                                               Height="16" />
                        <TextBlock Text="Inside"
                                   FontSize="14"
                                   FontWeight="SemiBold" />
                    </StackPanel>

                    <!-- 数据项 -->
                    <StackPanel Spacing="8">
                        <!-- 温度 -->
                        <Grid ColumnDefinitions="*,Auto">
                            <StackPanel Orientation="Horizontal" Spacing="6" Grid.Column="0">
                                <avalonia:MaterialIcon Kind="Temperature"
                                                       Width="14"
                                                       Height="14" />
                                <TextBlock Text="Temp"
                                           FontSize="12"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="{Binding InsideTemperature}"
                                       FontSize="12"
                                       FontWeight="Medium"
                                       Grid.Column="1"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center" />
                        </Grid>

                        <!-- 湿度 -->
                        <Grid ColumnDefinitions="*,Auto">
                            <StackPanel Orientation="Horizontal" Spacing="6" Grid.Column="0">
                                <avalonia:MaterialIcon Kind="Humidity"
                                                       Width="14"
                                                       Height="14" />
                                <TextBlock Text="Humidity"
                                           FontSize="12"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="{Binding InsideHumidity}"
                                       FontSize="12"
                                       FontWeight="Medium"
                                       Grid.Column="1"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center" />
                        </Grid>

                        <!-- 电量 -->
                        <Grid ColumnDefinitions="*,Auto">
                            <StackPanel Orientation="Horizontal" Spacing="6" Grid.Column="0">
                                <avalonia:MaterialIcon Kind="Battery"
                                                       Width="14"
                                                       Height="14" />
                                <TextBlock Text="Power"
                                           FontSize="12"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="{Binding InsidePower}"
                                       FontSize="12"
                                       FontWeight="Medium"
                                       Grid.Column="1"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center" />
                        </Grid>
                        
                        <TextBlock Text="{Binding InsideReportTime}"
                                   FontSize="12"
                                   Margin="0,10,0,0"
                                   FontWeight="Medium"
                                   TextTrimming="{x:Static TextTrimming.CharacterEllipsis}"
                                   TextWrapping="NoWrap"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- 室外传感器卡片 -->
            <Border MinWidth="160" Grid.Column="2"
                    CornerRadius="12"
                    Padding="16"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    BorderBrush="{DynamicResource SystemControlBackgroundBaseMediumBrush}"
                    BorderThickness="1">
                <StackPanel Spacing="12">
                    <!-- 标题 -->
                    <StackPanel Orientation="Horizontal" Spacing="6" HorizontalAlignment="Center">
                        <avalonia:MaterialIcon Kind="Nature"
                                               Width="16"
                                               Height="16" />
                        <TextBlock Text="Outside"
                                   FontSize="14"
                                   FontWeight="SemiBold" />
                    </StackPanel>

                    <!-- 数据项 -->
                    <StackPanel Spacing="8">
                        <!-- 温度 -->
                        <Grid ColumnDefinitions="*,Auto">
                            <StackPanel Orientation="Horizontal" Spacing="6" Grid.Column="0">
                                <avalonia:MaterialIcon Kind="Temperature"
                                                       Width="14"
                                                       Height="14" />
                                <TextBlock Text="Temp"
                                           FontSize="12"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="{Binding OutsideTemperature}"
                                       FontSize="12"
                                       FontWeight="Medium"
                                       Grid.Column="1"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center" />
                        </Grid>

                        <!-- 湿度 -->
                        <Grid ColumnDefinitions="*,Auto">
                            <StackPanel Orientation="Horizontal" Spacing="6" Grid.Column="0">
                                <avalonia:MaterialIcon Kind="Humidity"
                                                       Width="14"
                                                       Height="14" />
                                <TextBlock Text="Humidity"
                                           FontSize="12"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="{Binding OutsideHumidity}"
                                       FontSize="12"
                                       FontWeight="Medium"
                                       Grid.Column="1"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center" />
                        </Grid>

                        <!-- 电量 -->
                        <Grid ColumnDefinitions="*,Auto">
                            <StackPanel Orientation="Horizontal" Spacing="6" Grid.Column="0">
                                <avalonia:MaterialIcon Kind="Battery"
                                                       Width="14"
                                                       Height="14" />
                                <TextBlock Text="Power"
                                           FontSize="12"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="{Binding OutsidePower}"
                                       FontSize="12"
                                       FontWeight="Medium"
                                       Grid.Column="1"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center" />
                        </Grid>
                        
                        <TextBlock Text="{Binding OutsideReportTime}"
                                   FontSize="12"
                                   FontWeight="Medium"
                                   Margin="0,10,0,0"
                                   HorizontalAlignment="Center"
                                   TextTrimming="{x:Static TextTrimming.CharacterEllipsis}"
                                   TextWrapping="NoWrap"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- 空气传感器卡片 -->
        <Grid Grid.Row="1" Margin="20" VerticalAlignment="Center" ColumnDefinitions="*"
              IsVisible="{Binding ToggleVisibility}" >
            <Border MinWidth="160" Grid.Column="0"
                CornerRadius="12"
                Padding="16"
                Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                BorderBrush="{DynamicResource SystemControlBackgroundBaseMediumBrush}"
                BorderThickness="1">
            <StackPanel Spacing="12" HorizontalAlignment="Center">
                <!-- 标题 -->
                <StackPanel Orientation="Horizontal" Spacing="6" HorizontalAlignment="Center">
                    <avalonia:MaterialIcon Kind="AirPurifier"
                                           Width="16"
                                           Height="16" />
                    <TextBlock Text="Air Sensor"
                               FontSize="14"
                               FontWeight="SemiBold" />
                </StackPanel>

                <!-- 数据项 -->
                <Grid ColumnDefinitions="Auto,Auto" ColumnSpacing="20" RowSpacing="10" RowDefinitions="Auto,Auto">
                    <!-- 左列 -->
                    <StackPanel Grid.Row="0" Spacing="12" Grid.Column="0">
                        <!-- co2 -->
                        <Grid ColumnDefinitions="*,Auto" Grid.ColumnSpacing="15">
                            <StackPanel Orientation="Horizontal" Spacing="6" Grid.Column="0">
                                <avalonia:MaterialIcon Kind="MoleculeCo2"
                                                       Width="14"
                                                       Height="14" />
                                <TextBlock Text="CO₂"
                                           FontSize="12"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="{Binding DarkinCO2}"
                                       FontSize="12"
                                       FontWeight="Medium"
                                       Grid.Column="1"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center" />
                        </Grid>

                        <!-- pm2.5 -->
                        <Grid ColumnDefinitions="*,Auto">
                            <StackPanel Orientation="Horizontal" Spacing="6" Grid.Column="0">
                                <avalonia:MaterialIcon Kind="Cloud"
                                                       Width="14"
                                                       Height="14" />
                                <TextBlock Text="PM2.5"
                                           FontSize="12"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="{Binding DarkinPM25}"
                                        FontSize="12"
                                        FontWeight="Medium"
                                        Grid.Column="1"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center" />
                        </Grid>
                        
                        <!-- pm1 -->
                        <Grid ColumnDefinitions="*,Auto">
                            <StackPanel Orientation="Horizontal" Spacing="6" Grid.Column="0">
                                <avalonia:MaterialIcon Kind="WeatherHazy"
                                                       Width="14"
                                                       Height="14" />
                                <TextBlock Text="PM1"
                                           FontSize="12"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="{Binding DarkinPM1}"
                                       FontSize="12"
                                       FontWeight="Medium"
                                       Grid.Column="1"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center" />
                        </Grid>
                        
                        <!-- pm10 -->
                        <Grid ColumnDefinitions="*,Auto">
                            <StackPanel Orientation="Horizontal" Spacing="6" Grid.Column="0">
                                <avalonia:MaterialIcon Kind="Smog"
                                                       Width="14"
                                                       Height="14" />
                                <TextBlock Text="PM10"
                                           FontSize="12"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="{Binding DarkinPM10}"
                                       FontSize="12"
                                       FontWeight="Medium"
                                       Grid.Column="1"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center" />
                        </Grid>
                    </StackPanel>
                    
                    <!-- 右列 -->
                    <StackPanel Grid.Row="0" Spacing="12" Grid.Column="1">
                        <!-- tvoc -->
                        <Grid ColumnDefinitions="*,Auto" Grid.ColumnSpacing="15">
                            <StackPanel Orientation="Horizontal" Spacing="6" Grid.Column="0">
                                <avalonia:MaterialIcon Kind="ChemicalWeapon"
                                                       Width="14"
                                                       Height="14" />
                                <TextBlock Text="TVOC"
                                           FontSize="12"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="{Binding DarkinTVOC}"
                                       FontSize="12"
                                       FontWeight="Medium"
                                       Grid.Column="1"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center" />
                        </Grid>

                        <!-- temp -->
                        <Grid ColumnDefinitions="*,Auto">
                            <StackPanel Orientation="Horizontal" Spacing="6" Grid.Column="0">
                                <avalonia:MaterialIcon Kind="Thermometer"
                                                       Width="14"
                                                       Height="14" />
                                <TextBlock Text="Temperature"
                                           FontSize="12"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="{Binding DarkinTemperature}"
                                       FontSize="12"
                                       FontWeight="Medium"
                                       Grid.Column="1"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center" />
                        </Grid>
                        
                        <!-- humi -->
                        <Grid ColumnDefinitions="*,Auto">
                            <StackPanel Orientation="Horizontal" Spacing="6" Grid.Column="0">
                                <avalonia:MaterialIcon Kind="WaterPercent"
                                                       Width="14"
                                                       Height="14" />
                                <TextBlock Text="Humidity"
                                           FontSize="12"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="{Binding DarkinHumidity}"
                                       FontSize="12"
                                       FontWeight="Medium"
                                       Grid.Column="1"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center" />
                        </Grid>
                    </StackPanel>
                    
                    <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2">
                        <TextBlock Text="{Binding DarkinReportTime}" 
                                   FontSize="12"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>
        </Grid>

        <!-- 图表区域 - 占满剩余空间 -->
        <Grid Grid.Row="2" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- 图表控制区域 -->
            <StackPanel Grid.Row="0" Margin="0,0,0,10">
                <Grid RowDefinitions="Auto, Auto" ColumnDefinitions="Auto,*"
                      ColumnSpacing="20" RowSpacing="10">
                    <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" 
                                Spacing="20"
                                HorizontalAlignment="Center"
                                Orientation="Horizontal">
                        <CalendarDatePicker SelectedDate="{Binding ChartStartFrom}"/>
                        <Label Content="To" VerticalAlignment="Center" />
                        <CalendarDatePicker SelectedDate="{Binding ChartEndFrom}"/>
                    </StackPanel>
                    
                    <Label Grid.Row="1" Grid.Column="0" Content="For: " VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="1" Spacing="10">
                        <ComboBox ItemsSource="{Binding SensorOptions}"
                                  SelectedItem="{Binding ChartSensor}" 
                                  HorizontalAlignment="Left"
                                  MinWidth="100"/>
                        <ComboBox ItemsSource="{Binding SensorDataTypeOptions}"
                                  SelectedItem="{Binding ChartSensorDataType}" 
                                  HorizontalAlignment="Left"
                                  MinWidth="100"/>
                        
                        <Button Margin="10,0,0,0"
                                Command="{Binding RefreshChart}"
                                HorizontalAlignment="Center">
                            <StackPanel Orientation="Horizontal" Spacing="5">
                                <avalonia:MaterialIcon Kind="Refresh"
                                                       Width="20"
                                                       Height="20"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </StackPanel>
            
            <!-- 图表内容 - 占满剩余空间 -->
            <ContentControl Grid.Row="1"
                            Content="{Binding PlotControl, Mode=OneTime}"
                            VerticalAlignment="Stretch"
                            HorizontalAlignment="Stretch"/>
        </Grid>
    </Grid>
</UserControl>